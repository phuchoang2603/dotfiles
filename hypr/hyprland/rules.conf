# Zen Browser → workspace 1
windowrule = workspace 1, class:(zen)
# Kitty (assuming class is 'kitty', you can run hyprctl clients to confirm)
windowrule = workspace 2, class:(kitty)
# Visual Studio Code → workspace 3
windowrule = workspace 3, class:(Code)
# Spotify (includes both official and adblock versions if class matches)
windowrule = workspace 4, class:(Spotify)
# Obsidian → workspace 5
windowrule = workspace 5, class:(obsidian)
# GNOME Files (Nautilus) → workspace 6
windowrule = workspace 6, class:(org.gnome.Nautilus)
# maybe add rule to auto move certain apps to scratch workspace
